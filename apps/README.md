# Apps

Client applications live here.

- `web`: current Next.js web command center.
- `desktop_mobile`: current Flutter desktop/mobile command center.

Both clients must consume the same canonical event stream and render the same
A2UI surfaces from `packages/protocol`.
