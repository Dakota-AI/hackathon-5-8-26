# Apps

Client applications live here.

- `web-next`: Next.js web client.
- `flutter`: Flutter mobile and desktop client.
- `desktop_mobile`: current Flutter desktop/mobile console scaffold.

Both clients must consume the same canonical event stream and render the same A2UI surfaces from `packages/protocol`.
