# Flutter App

Flutter mobile and desktop client placeholder.

Initial responsibilities:

- Share protocol models generated from `packages/protocol`.
- Connect to the same Cloudflare realtime plane as the web app.
- Render messages, run status, notifications, approvals, and A2UI surfaces.
- Support offline/reconnect replay using server sequence cursors.
