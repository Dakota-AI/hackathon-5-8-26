# Web App

Next.js web client placeholder.

Initial responsibilities:

- Authenticate through Amplify/Cognito.
- Connect to the Cloudflare realtime plane over WebSocket.
- Render canonical run/task events.
- Render A2UI surfaces from allowlisted catalogs.
- Send approval decisions and user commands through the control API or realtime command bridge.
