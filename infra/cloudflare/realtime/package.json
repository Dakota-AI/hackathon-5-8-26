{
  "name": "@agents-cloud/cloudflare-realtime",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cloudflare Worker and Durable Objects realtime fanout plane for Agents Cloud.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test dist/test/**/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
