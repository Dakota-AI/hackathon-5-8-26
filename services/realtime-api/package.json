{
  "name": "@agents-cloud/realtime-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "AWS-native WebSocket realtime handlers for Agents Cloud events and notifications.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test dist/test/**/*.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.821.0",
    "@aws-sdk/client-dynamodb": "^3.821.0",
    "@aws-sdk/lib-dynamodb": "^3.821.0",
    "@aws-sdk/util-dynamodb": "^3.821.0",
    "aws-jwt-verify": "^5.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
