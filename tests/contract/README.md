# Contract Tests

Contract tests should validate:

- Canonical event envelopes.
- Type-specific event payloads.
- Replay and gap repair behavior.
- Next.js and Flutter protocol compatibility.
- Backward-compatible schema evolution.

The first executable schema validation lives in `packages/protocol`.
